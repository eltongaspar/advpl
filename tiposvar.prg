Function Main()

    Local nNumero := 0                  // Numerico
   // Local aNotas  := { 4, 9, 10, 6.8 }  // Array
    Local lLogico := .T.                // Logico (.T. / .F.)
    Local cNome   := "Elton Moura"         // Caracter
    Local dHoje   := CtoD("01/01/01")   // Data
  //  Local bBloco  := { |x| x := x+1 }   // Bloco de Código

    Qout(nNumero)
    Qout(lLogico)
    Qout(cNome)
    Qout(dHoje)

RETURN nil
